#!/bin/bash

TEST_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/.."

PLATFORM_HOST=head
CLARA_SERVICES=$1
export CLARA_SERVICES

$CLARA_SERVICES/bin/clara-dpe -host $PLATFORM_HOST | tee $TEST_DIR/log/$HOSTNAME.log
