#!/bin/bash

TEST_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/.."

PLATFORM_HOST=$1
CLARA_SERVICES=$2
export CLARA_SERVICES

$CLARA_SERVICES/bin/clara-dpe -host $PLATFORM_HOST 2>&1 | tee $TEST_DIR/log/$HOSTNAME.log
